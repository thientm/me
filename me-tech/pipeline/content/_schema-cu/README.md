# Kịch bản viết theo schema CŨ (trước 17.09.2026)

Hai file này dùng `scene` + `show.type`, không chạy được với pipeline hiện tại.
Giữ lại vì nội dung vẫn dùng được — muốn dựng lại thì viết lại theo
`../lawzero.json` (mode + group + words/weights).

- `gemini-live.json` — chuẩn bị 16/09, chưa đăng
- `novo-anthropic.json` — đã render `render/novo-anthropic.mp4`, chờ duyệt
